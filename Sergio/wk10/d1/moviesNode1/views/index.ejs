<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>movies Node</title>
  </head>
  <body>

    <h2>Movies with Node</h2>
    <form action="/" method="get">
      <input id="movie" type="text" name="name" placeholder="Enter movie title">
      <button id='submitMovie' type="submit">Search</button>
    </form>
    <div id='movies'>
      <% if (movies != undefined) { %>
        <% movies.forEach(function(movie) { %>
          <p><%= movie.title %></p>
        <% }); %>
      <% } %>
    </div>

    <script src="https://code.jquery.com/jquery-3.1.1.min.js" charset="utf-8"></script>
    <script type="text/javascript">
      // $('#submitMovie').click(searchMovie);
      //
      // function searchMovie(e) {
      //   e.preventDefault();
      //   var movieName = $("#movie").val();
      //   $.get(`/movies?name=${movieName}`).done(function(response) {
      //     response.forEach(function(movie) {
      //       $('#movies').append(`<p>${movie.title}</p>`);
      //     });
      //   });
      // }

    </script>
  </body>
</html>
